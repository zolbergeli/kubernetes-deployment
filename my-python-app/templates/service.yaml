apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Chart.Name }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001  
  selector:
    app: {{ .Chart.Name }}

